node_modules/
lib/
*.tgz
.npm-cache/
.dsh-web.log
.dsh-runtime.json
.window-events.log
tools/out/
__pycache__/

# Publishing private keys/certificate bundles must stay outside the repository.
*.pem
*.pfx
*.p12

# Evaluation run outputs stay outside the repository by default. Ignore any
# in-repository scratch runs; keep private holdouts outside this checkout.
evaluation/product-modeling-dev-v1/runs/

# Local research scratch: decompressed session traces (private conversation
# data) and fetched docs — never commit.
.research/
.dsh-houdini-media/
.dsh-houdini-results/
.dsh-vision-router/
.dsh-vision-toolkit/
session/
